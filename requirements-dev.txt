-r requirements.txt

# Development-only auditing tools.
bandit[toml]>=1.9.4
safety>=3.8.1

# Keep these transitive packages explicitly on fixed versions so Dependabot can
# reason about secure minimums without a lockfile.
authlib>=1.7.2
cryptography>=49.0.0
filelock>=3.29.4
marshmallow>=4.3.0
nltk>=3.9.4
